# Benchmark-to-Deployment Validity Mapper

Map what a benchmark score actually predicts about real deployment.

Dimensions:
task horizon, interactivity, environmental determinism, tool use, memory, safety, robustness, latency, cost and distribution shift.

Output:
which deployment claims are supported, weakly supported or not justified by benchmark evidence.
