# Scientific Agent Trajectory Verifier

Evaluate not only an agent's final answer but the scientific process it followed.

Verify:
data provenance, code execution, model choice, assumptions, intermediate calculations, tool calls, contamination, result reproducibility and evidence citations.

Output:
verified / partially verified / invalid trajectory with exact failure points.
