# Sensor-Fusion Observability Planner

Given hidden states, candidate sensors, models and failure modes, determine which states are actually observable and which sensor additions have the highest information value.

Outputs:
- observability map
- redundant vs unique sensors
- failure-tolerant coverage
- uncertainty after sensor loss
- best next sensor or placement

**Business angle:** avoid over-instrumentation while preserving decision quality.
