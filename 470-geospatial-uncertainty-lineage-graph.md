# Geospatial Uncertainty & Lineage Graph

Track uncertainty and provenance through geospatial pipelines.

`sensor -> correction -> resampling -> fusion -> model -> map -> zonal statistic -> decision`

Show where spatial uncertainty, missing coverage or resolution mismatch enters and which downstream claims are affected.
