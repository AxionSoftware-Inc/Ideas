# Validity Algebra & Compositional Trust Kernel

Define formal rules for composing validity domains across scientific models, sensors and transformations.

Example:
model A valid for temperature range X;
sensor B calibrated in range Y;
surrogate C trained on subset Z.

The system computes the validity of the composed pipeline rather than letting each component claim trust independently.

This can become a foundational Scientific OS primitive.
