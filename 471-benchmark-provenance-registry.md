# Benchmark Provenance Registry

Create a registry that treats a benchmark as a versioned scientific artifact.

Store:
task origin, source data, licensing, generation process, hidden/public status, contamination risk, evaluator, environment, scoring code, known flaws and deployment relevance.

Goal: benchmark scores become interpretable evidence rather than leaderboard numbers detached from context.
