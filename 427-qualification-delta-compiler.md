# AM Qualification Delta Compiler

When a parameter, machine component, powder lot, geometry or post-process changes, compute the smallest justified requalification scope.

`change -> affected mechanisms -> affected evidence -> reusable evidence -> required tests`

This directly targets the current cost of treating small process changes as full requalification events.
